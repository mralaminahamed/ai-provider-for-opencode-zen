# Git
.git/
.gitattributes
.gitignore

# CI / tooling config — must never appear in the plugin zip
.distignore
.editorconfig
.github/
phpcs.xml.dist
phpstan.neon.dist
phpunit.xml.dist
phpunit.xml
phpcs.xml
phpstan.neon
*.dist

# Tests
tests/

# Composer (phar only; composer.json + composer.lock ship with the plugin)
composer.phar

# IDE
.idea/
.vscode/
*.sublime-*

# OS
.DS_Store
Thumbs.db

# Claude Code / AI tools
.claude/
.remember/
.playwright-mcp/
session-ses_*.md
CLAUDE.md
AGENTS.md

# WP.org assets (uploaded separately via SVN /assets/, not in plugin zip)
.wordpress-org/

# Build output
release/
build/
dist/
*.zip

# Documentation (not needed in plugin zip)
docs/

# Markdown (readme.txt is canonical for WP.org)
*.md
LICENSE

